{
  "name": "javjump",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:release": "tsc && vite build --mode release",
    "release": "npm run build:release && node script/release.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.2",
    "vite-plugin-monkey": "^5.0.8"
  }
}
